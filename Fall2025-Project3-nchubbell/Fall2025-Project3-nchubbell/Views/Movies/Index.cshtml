@model IEnumerable<Fall2025_Project3_nchubbell.Models.Movie>
@using System

@{
    ViewData["Title"] = "Movies";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="mb-0" style="color:#9E1B32;">Movies</h1>
        <a asp-action="Create"
           class="btn btn-sm"
           style="background-color:#9E1B32; color:#f8f9fa;">
            Create New
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <table class="table table-striped table-hover align-middle" id="moviesTable">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>IMDb</th>
                        <th>Genre</th>
                        <th>Year</th>
                        <th>Poster</th>
                        <th style="width: 140px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.Title
                            </td>
                            <td>
                                @if (!string.IsNullOrEmpty(item.ImdbUrl))
                                {
                                    <a href="@item.ImdbUrl"
                                       target="_blank"
                                       rel="noopener noreferrer"
                                       class="link-underline">
                                        IMDb
                                    </a>
                                }
                                else
                                {
                                    <span class="text-muted">N/A</span>
                                }
                            </td>
                            <td>
                                @item.Genre
                            </td>
                            <td>
                                @item.Year
                            </td>
                            <td>
                                @if (item.Poster != null)
                                {
                                    var base64 = Convert.ToBase64String(item.Poster);
                                    var imgSrc = $"data:image/png;base64,{base64}";
                                    <img src="@imgSrc"
                                         alt="Poster for @item.Title"
                                         style="height:60px; width:auto;"
                                         class="rounded shadow-sm" />
                                }
                                else
                                {
                                    <span class="text-muted small">No poster</span>
                                }
                            </td>
                            <td>
                                <div class="btn-group btn-group-sm" role="group">
                                    <a asp-action="Edit" asp-route-id="@item.Id"
                                       class="btn btn-outline-secondary">
                                        Edit
                                    </a>
                                    <a asp-action="Details" asp-route-id="@item.Id"
                                       class="btn btn-outline-primary">
                                        Details
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.Id"
                                       class="btn btn-outline-danger">
                                        Delete
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
